#include <stdio.h>

int invert(int x, int p, int n);

int invert(int x, int p, int n)
{

}

int main(int argc, char[] *argv)
{

  if(argc != 4) {
    puts("invert requires 3 arguments, an bit-vector to invert, the position at which inversion should begin, and the number of bits to invert.");
    return 1;
  }


  return 0;
}
